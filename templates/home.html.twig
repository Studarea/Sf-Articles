{% extends 'base.html.twig' %}



{% block content %}
    <main>

        <section>

            {% for article in articles %}

                <div class="container">
                    <div class="container-content">
                            <a href="{{ path('article_page', {'id': article.id}) }}">{{ article.title }}</a>
                    </div>
                </div>
            {% endfor %}

        </section>

   </main>

    <div id="home-background"></div>

{% endblock content %}